@model Scienter.HotelERP.Common.Domain.UserWiseProperty

@{
    ViewBag.Title = "Create / Edit";
}

<div class="col-md-12">

    <form id="userWiseProperties">
        @Html.AntiForgeryToken()
     
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <input type="hidden" value="@Model.Id" name="Id" id="Id" />
            <div class="row">
                <div class="col-md-3">
                    <div class="form-group label-floating">
                        <label class="control-label">User *</label>
                        @{Html.RenderAction("UsersCombo", "Users", new { name = "UserId", requiered = true, value = Model?.UserId });}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="form-group label-floating">
                        <label class="control-label">Properties </label>
                        <div id="userWisePropertiesContainer">
                            @{Html.RenderAction("LoadUserWiseProperties", "UserWiseProperties", new { userId = 0 });}
                        </div>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <input type="submit" id="btnSave" value="Save" class="btn btn-primary" />
                &nbsp;
                <input type="reset" id="btnClear" value="Clear" class="btn btn-default" />
            </div>
    </form>
</div>


@* Select Index change in combo box*@

<script>
    $(document).ready(function () {
        var userId = 0;
        userId = GetParameterValues("userId");
        if (userId != 0) {
            LoadUserWisePropertise(userId)
        } else {
        }

    });


    function LoadUserWisePropertise(userId) {

        $.ajax({
            url: '/useraccess/userwiseproperties/LoadUserWiseProperties?userId=' + userId,
            dataType: 'html',
            success: function (data) {
                $('#UserId').val(userId);
                $('#userWisePropertiesContainer').html(data);

            },

        });
    }







    $('#UserId').change(function () {
      if ($(this).val() > 0) {

        $.ajax({
            url: '/useraccess/userwiseproperties/LoadUserWiseProperties?userId=' + $(this).val(),
            dataType: 'html',
            success: function (data) {
                $('#userWisePropertiesContainer').html(data);
            },

        });
      } else {
          $('input:checkbox').removeAttr('checked');
      }
    });
</script>


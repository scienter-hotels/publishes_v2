@model IEnumerable < Scienter.HotelERP.Common.Domain.TransferType>

@{
    ViewBag.Title = "Existing Transfer Types";
}

<div class="table-responsive">
       <table class="table table-striped"
                   id="existingTransferTypeGrid" 

              >
           <thead>
               <tr>
                   <th class="text-primary">
                       @Html.DisplayNameFor(model => model.Name)
                   </th>
                   <th class="text-primary">
                       @Html.DisplayNameFor(model => model.Code)
                   </th>
                   <th class="text-primary">
                       From Location
                   </th>
                   <th class="text-primary">
                       To Location
                   </th>
                   <th class="col-md-1 text-primary">
                       Active
                   </th>
                   <th class="col-md-1 hidden-print"></th>
               </tr>
           </thead>

           <tbody>
               @foreach (var item in Model)
               {
                   <tr>
                       <td>
                           @Html.DisplayFor(modelItem => item.Name)
                       </td>
                       <td>
                           @Html.DisplayFor(modelItem => item.Code)
                       </td>
                       <td>
                           @Html.DisplayFor(modelItem => item.FromLocation)
                       </td>
                       <td>
                           @Html.DisplayFor(modelItem => item.ToLocation)
                       </td>
                       <td class="col-md-1">
                           <div class="form-group checkbox">
                               <label>
                                   @Html.DisplayFor(modelItem => item.IsActive)
                                   <span class="hidden">
                                       @if (item.IsActive)
                                       {
                                           @:Active
                                    }
                                       else
                                       {
                                           @:Inactive
                                    }
                                   </span>
                               </label>
                           </div>
                       </td>
                       <td class="hidden-print text-right">
                           @*<a class="btn btn-theme-color-2 btn-sm call-popup-model" id="btnPrint" data-title="Alert Type - @item.Name" data-href="/administration/transfertypes/print/@item.Id">
                               <span class="glyphicon glyphicon-print"></span>
                           </a>*@

                           <a class="btn btn-theme-color-2 btn-sm" id="btnEdit" data-ajax="true" data-ajax-mode="replace" data-ajax-update="#content" href="/administration/transfertypes/edit/@item.Id">
                               <span class="glyphicon glyphicon-pencil"></span>
                           </a>

                           <a class="btn btn-danger btn-sm call-popup-model-delete" id="btnDelete" data-href="/administration/transfertypes/delete/@item.Id">
                               <span class="glyphicon glyphicon-trash"></span>
                           </a>
                       </td>
                   </tr>
               }
           </tbody>
    </table>
</div>

<script>
    $(document).ready(function () {
        $('#existingTransferTypeGrid').DataTable(
            {
                //"dom": '<"top"ifb>rt<"bottom"lp><"clear">',
                searching: true,
                ordering: true,
                //"lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "All"]],
                dom: 'Bfrtipl',
                select: true,
                buttons: [
                            'copy',
                            'excel',
                            'csv',
                            'pdf',
                            'print'
                ],
                scrollY: '58vh',
            } );
    });
</script>
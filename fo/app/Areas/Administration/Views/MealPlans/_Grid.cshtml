@model  IEnumerable<Scienter.HotelERP.Common.Domain.MealPlan>

@{
    ViewBag.Title = "Existing Meal Plans";
}

<div class="table-responsive">
    <table class="table table-striped"
           id="existingMealPlanGrid">
        <thead>
            <tr>
                <th class="col-md-2 text-primary">
                    @Html.DisplayNameFor(model => model.Prefix)
                </th>
                <th class="  text-primary">
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th class="number  text-primary">
                    @Html.DisplayNameFor(model => model.FoodAllocation)
                </th>
                <th class="number  text-primary">
                    @Html.DisplayNameFor(model => model.BevAllocation)
                </th>
                <th> </th>
                <th class="col-md-1  text-primary">
                    @Html.DisplayNameFor(model => model.DisplayOrder)
                </th>
                <th class="col-md-1 text-primary">
                    BE.
                </th>
                <th class="col-md-1 text-primary">
                    @Html.DisplayNameFor(model => model.IsActive)
                </th>

                <th class="col-md-1 hidden-print"></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Prefix)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td class="number">
                    @Html.DisplayFor(modelItem => item.FoodAllocation)
                </td>
                <td class="number">
                    @Html.DisplayFor(modelItem => item.BevAllocation)
                </td>
                <td></td>
                <td>
                    @Html.DisplayFor(modelItem => item.DisplayOrder)
                </td>
                <td>
                    <div class="form-group checkbox">
                        <label>
                            @Html.DisplayFor(modelItem => item.IsApplicableForIBE)
                            <span class="hidden">
                                @if (item.IsApplicableForIBE)
                                {
                                    @:Active
                                }
                                else
                                {
                                    @:Inactive
                                }
                            </span>
                        </label>
                    </div>



                </td>
                <td class="col-md-1">
                    <div class="form-group checkbox">
                        <label>
                            @Html.DisplayFor(modelItem => item.IsActive)
                            <span class="hidden">
                                @if (item.IsActive)
                                {
                                    @:Active
                                }
                                else
                                {
                                    @:Inactive
                                }
                            </span>
                        </label>
                    </div>

                </td>

                <td class="hidden-print text-right">
                    @*<a class="btn btn-theme-color-2 btn-sm call-popup-model" id="btnPrint" data-title="MealPlan - @item.Name" data-href="/administration/mealplans/print/@item.Id">
                <span class="glyphicon glyphicon-print"></span>
            </a>*@

                    <a class="btn btn-theme-color-2 btn-sm" id="btnEdit" data-ajax="true" data-ajax-mode="replace" data-ajax-update="#content" href="/administration/mealplans/edit/@item.Id">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>

                    <a class="btn btn-danger btn-sm call-popup-model-delete" id="btnDelete" data-href="/administration/mealplans/delete/@item.Id">
                        <span class="glyphicon glyphicon-trash"></span>
                    </a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>

<script>
    $(document).ready(function () {
        $('#existingMealPlanGrid').DataTable(
            {
                //"dom": '<"top"ifb>rt<"bottom"lp><"clear">',
                searching: true,
                ordering: true,
                //"lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "All"]],
                dom: 'Bfrtipl',
                select: true,
                buttons: [
                            'copy',
                            'excel',
                            'csv',
                            'pdf',
                            'print'
                ],
                scrollY: '58vh',
            } );
    });
</script>